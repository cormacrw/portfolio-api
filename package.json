{
  "name": "portfolio-api",
  "version": "1.0.0",
  "description": "api for portfolio site",
  "main": "index.js",
  "scripts": {
    "deploy": "rm -rf build/ && tsc -p . && sls deploy",
    "build": "tsc -p ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "serverless-http": "^3.1.1"
  },
  "devDependencies": {
    "@aws-sdk/types": "^3.310.0",
    "@types/aws-lambda": "^8.10.114",
    "@types/express": "^4.17.17",
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1362.0",
    "typescript": "^5.0.4"
  }
}
